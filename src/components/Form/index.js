import React from 'react';

const Form = () => (
    <form className="app__form">
        <input type="text" className="app__search" />
        <button className="app__submit">Get Data</button>
    </form>
);

export default Form;